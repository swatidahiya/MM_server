import { EmailOptions } from "./email-util";
import { OnInit, OnChanges, SimpleChanges } from "@angular/core";
import { Validator, AbstractControl, ValidationErrors } from "@angular/forms";
export declare class EmailValidatorDirective implements Validator, OnInit, OnChanges {
    email: "normal" | "simple";
    private validator;
    private onChange;
    ngOnInit(): void;
    setValidator(type: string): void;
    ngOnChanges(changes: SimpleChanges): void;
    registerOnValidatorChange?(fn: () => void): void;
    validate(c: AbstractControl): ValidationErrors;
}
export declare class EmailSuggestValidatorDirective implements Validator, OnInit, OnChanges {
    emailSuggest: EmailOptions;
    private validator;
    private onChange;
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    registerOnValidatorChange?(fn: () => void): void;
    validate(c: AbstractControl): ValidationErrors;
}
