import { AbstractControl, ValidatorFn, ValidationErrors } from "@angular/forms";
export declare class UniversalValidators {
    static noWhitespace(control: AbstractControl): {
        [key: string]: boolean;
    };
    static noEmptyString(control: AbstractControl): {
        [key: string]: boolean;
    };
    static isNumber(control: AbstractControl): {
        [key: string]: boolean;
    };
    static isInRange(minValue: number, maxValue: number): ValidatorFn;
    static minLength(minLength: number): (control: AbstractControl) => ValidationErrors;
    static maxLength(maxLength: number): (control: AbstractControl) => ValidationErrors;
    static min(min: number): (control: AbstractControl) => ValidationErrors;
    static max(max: number): (control: AbstractControl) => ValidationErrors;
}
