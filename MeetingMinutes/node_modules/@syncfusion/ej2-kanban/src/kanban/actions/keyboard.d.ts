import { Kanban } from '../base';
/**
 * Drag and Drop module is used to perform card actions.
 * @hidden
 */
export declare class Keyboard {
    private parent;
    private keyboardModule;
    private keyConfigs;
    /**
     * Constructor for keyboard module
     * @private
     */
    constructor(parent: Kanban);
    private keyActionHandler;
    private processCardSelection;
    private processLeftRightArrow;
    private processUpDownArrow;
    private processColumnExpandcollapse;
    private processSwimlaneExpandCollapse;
    private getCardId;
    private processNextRow;
    private processPreviousRow;
    private processCards;
    private processEnter;
    private processTab;
    private processMoveCards;
    /**
     * Get module name.
     */
    protected getModuleName(): string;
    /**
     * To destroy the keyboard module.
     * @return {void}
     * @private
     */
    destroy(): void;
}
